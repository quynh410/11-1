let a = 6;
let result = ++a + a + a++ + ++a + a;

// Tiền tố (++a):giá trị của biến sẽ được tăng lên một đơn vị ngay trong biểu thức
// Hậu tố (a++):Giá trị của biến sẽ được tăng lên một đơn vị ngay sau khi biểu thức kết thúc